1. Open Terminal
2. Change directory to the project folder using "cd path_to_folder"
3. Copy all the json files to the "jsons" folder inside project folder
4. In terminal run "python script.py" command, if it is not worked use "py script.py"
5. It will run the programme and save the csv as output.csv